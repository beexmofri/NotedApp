package com.example.roomapp

import org.junit.Assert.*

import org.junit.Test

class ValidatorTest {

    @Test
    fun validateEmail() {
        val amount = 20
        val result = Validator.ValidateInput(amount)
        assertThat
    }
}